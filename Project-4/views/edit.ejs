<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Book</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            background: linear-gradient(135deg, #94bbe9, #b7eaff);
            min-height: 100vh;
        }

        header {
            background: linear-gradient(135deg, #051933, #073a61);
            padding: 30px;
            font-size: 2.6rem;
            text-align: center;
            font-weight: 700;
            letter-spacing: 2px;
            color: white;
            box-shadow: 0 6px 30px rgba(0, 0, 0, 0.25);
        }

        .form-container {
            max-width: 760px;
            margin: 60px auto;
            padding: 40px;
            border-radius: 25px;
            background: rgba(255, 255, 255, 0.6);
            backdrop-filter: blur(10px);
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.25);
        }

        table {
            width: 100%;
            border-spacing: 0 20px;
        }

        td {
            font-weight: 700;
            font-size: 1.05rem;
            color: #022b35;
        }

        input,
        textarea {
            width: 100%;
            padding: 15px;
            font-size: 1rem;
            border-radius: 14px;
            border: 2px solid rgba(0, 0, 0, 0.2);
            transition: .3s;
        }

        input:focus,
        textarea:focus {
            border-color: #073a61;
            box-shadow: 0 0 6px rgba(0, 0, 0, 0.3);
        }

        textarea {
            height: 110px;
        }

        .btn-row {
            display: flex;
            gap: 20px;
            margin-top: 30px;
        }

        .btn {
            flex: 1;
            padding: 14px;
            font-size: 1.08rem;
            font-weight: 700;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: .3s;
        }

        .update-btn {
            background: linear-gradient(135deg, #106b83, #051933);
            color: white;
        }

        .update-btn:hover {
            transform: scale(1.05);
        }

        .back-btn {
            background: #444;
            color: white;
            text-align: center;
            text-decoration: none;
            line-height: 50px;
        }

        .back-btn:hover {
            background: #000;
        }

        footer {
            margin-top: 80px;
            background: #051933;
            padding: 22px;
            color: #d6d6d6;
            text-align: center;
        }
    </style>
</head>

<body>

    <header>✏ Edit Book</header>

    <div class="form-container">

        <form action="/edit/<%= book.id %>" method="post">

            <table>
                <tr>
                    <td>Title</td>
                    <td>
                        <input type="text" name="title" value="<%= book.title %>" required>
                    </td>
                </tr>

                <tr>
                    <td>Author</td>
                    <td>
                        <input type="text" name="author" value="<%= book.author %>" required>
                    </td>
                </tr>

                <tr>
                    <td>Genre</td>
                    <td>
                        <input type="text" name="genre" value="<%= book.genre %>" required>
                    </td>
                </tr>

                <tr>
                    <td>Price</td>
                    <td>
                        <input type="number" name="price" value="<%= book.price %>" required>
                    </td>
                </tr>

                <tr>
                    <td>Description</td>
                    <td>
                        <textarea name="description" required>
<%= book.description %></textarea>
                    </td>
                </tr>

                <tr>
                    <td>Publisher</td>
                    <td>
                        <input type="text" name="publisher" value="<%= book.publisher %>" required>
                    </td>
                </tr>

                <tr>
                    <td>Year</td>
                    <td>
                        <input type="number" name="year" value="<%= book.year %>" required>
                    </td>
                </tr>
            </table>

            <div class="btn-row">
                <a href="/" class="btn back-btn"><i class="fas fa-arrow-left"></i> Back</a>
                <button type="submit" class="btn update-btn"><i class="fas fa-save"></i> Update</button>
            </div>

        </form>
    </div>

    <footer>
        <i class="fas fa-copyright"></i> 2025 Bookstore Management System.
    </footer>

</body>

</html>