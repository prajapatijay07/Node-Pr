<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Bookstore Management</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            background: linear-gradient(135deg, #b7eaff, #94bbe9);
            min-height: 100vh;
            color: #1e293b;
        }

        header {
            width: 100%;
            padding: 35px;
            text-align: center;
            font-size: 3rem;
            letter-spacing: 2px;
            font-weight: 700;
            color: #fff;
            background: linear-gradient(135deg, #051933, #073a61, #106b83);
            top: 0;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.25);
        }

        .form-container {
            max-width: 780px;
            margin: 50px auto;
            padding: 35px;
            border-radius: 25px;
            background: rgba(255, 255, 255, 0.55);
            backdrop-filter: blur(10px);
            box-shadow: 0 14px 50px rgba(0, 0, 0, 0.25);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .form-container h2 {
            text-align: center;
            font-size: 1.8rem;
            margin-bottom: 25px;
            color: #073a61;
        }

        table {
            width: 100%;
            border-spacing: 0 20px;
        }

        td {
            font-weight: 600;
            font-size: 1.05rem;
        }

        input,
        textarea {
            width: 100%;
            padding: 15px 16px;
            border-radius: 13px;
            border: 2px solid rgba(0, 0, 0, 0.1);
            background: #ffffffcc;
            transition: 0.3s;
            font-size: 1rem;
        }

        input:focus,
        textarea:focus {
            outline: none;
            border-color: #0a7f8a;
            box-shadow: 0 0 6px rgba(0, 0, 0, 0.25);
        }

        textarea {
            height: 110px;
        }

        .btn {
            width: 100%;
            padding: 15px;
            border: none;
            margin-top: 15px;
            font-size: 1.15rem;
            font-weight: 700;
            color: white;
            border-radius: 13px;
            background: linear-gradient(135deg, #106b83, #051933);
            transition: 0.3s;
            cursor: pointer;
        }

        .btn:hover {
            transform: scale(1.02);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
        }

        .list-table {
            width: 96%;
            margin: 65px auto;
            background: white;
            border-radius: 20px;
            overflow: hidden;
            border-collapse: collapse;
            box-shadow: 0 13px 40px rgba(0, 0, 0, 0.25);
        }

        .list-table th {
            background: linear-gradient(#073a61, #051933);
            padding: 18px;
            font-size: 1.05rem;
            color: white;
        }

        .list-table tr {
            transition: 0.3s;
        }

        .list-table tr:hover {
            background: rgba(0, 0, 0, 0.05);
            transform: scale(1.01);
        }

        .list-table td {
            padding: 18px;
            font-size: 1rem;
            border-bottom: 1px solid #e5e5e5;
        }

        .action-btn {
            padding: 10px 18px;
            border-radius: 8px;
            border: none;
            color: white;
            font-weight: 600;
            cursor: pointer;
        }

        .edit {
            background: linear-gradient(135deg, #106b83, #051933);
            color: #fff;
            border: none;
            padding: 10px 18px;
            margin: 10px;
            border-radius: 10px;
            font-weight: 600;
            transition: .3s;
        }

        .edit:hover {
            transform: scale(1.05);
            box-shadow: 0 0 12px rgba(16, 107, 131, 0.6);
        }

        .delete {
            background: linear-gradient(135deg, #b00020, #730013);
            color: #fff;
            border: none;
            padding: 10px 18px;
            border-radius: 10px;
            font-weight: 600;
            transition: .3s;
        }

        .delete:hover {
            transform: scale(1.05);
            box-shadow: 0 0 12px rgba(176, 0, 32, 0.6);
        }


        footer {
            margin-top: 80px;
            background: #051933;
            color: #cbd5e1;
            text-align: center;
            padding: 22px;
            font-size: 0.95rem;
        }
    </style>
</head>

<body>

    <header><i class="fas fa-book-open"></i> Bookstore Management</header>

    <div class="form-container">
        <h2>Add New Book</h2>

        <form action="/add-book" method="post">
            <table>
                <tr>
                    <td><i class="fas fa-book"></i> Title</td>
                    <td><input type="text" name="title" required></td>
                </tr>
                <tr>
                    <td><i class="fas fa-user"></i> Author</td>
                    <td><input type="text" name="author" required></td>
                </tr>
                <tr>
                    <td><i class="fas fa-tags"></i> Genre</td>
                    <td><input type="text" name="genre" required></td>
                </tr>
                <tr>
                    <td><i class="fas fa-money-bill"></i> Price</td>
                    <td><input type="number" name="price" required></td>
                </tr>
                <tr>
                    <td><i class="fas fa-align-left"></i> Description</td>
                    <td><textarea name="description" required></textarea></td>
                </tr>
                <tr>
                    <td><i class="fas fa-building"></i> Publisher</td>
                    <td><input type="text" name="publisher" required></td>
                </tr>
                <tr>
                    <td><i class="fas fa-calendar"></i> Published Year</td>
                    <td><input type="number" name="year" required></td>
                </tr>
                <tr>
                    <td colspan="2"><button class="btn">Add Book <i class="fas fa-plus"></i></button></td>
                </tr>
            </table>
        </form>
    </div>

    <table class="list-table">
        <tr>
            <th><i class="fas fa-hashtag"></i></th>
            <th><i class="fas fa-book"></i> Book</th>
            <th><i class="fas fa-user-pen"></i> Author</th>
            <th><i class="fas fa-layer-group"></i> Genre</th>
            <th><i class="fas fa-indian-rupee-sign"></i> Price</th>
            <th><i class="fas fa-align-left"></i> Description</th>
            <th><i class="fas fa-building"></i> Publisher</th>
            <th><i class="fas fa-calendar"></i> Year</th>
            <th><i class="fas fa-gear"></i> Action</th>
        </tr>

        <% books.forEach((book, index)=> { %>
            <tr>
                <td>
                    <%= index + 1 %>
                </td>
                <td>
                    <%= book.title %>
                </td>
                <td>
                    <%= book.author %>
                </td>
                <td>
                    <%= book.genre %>
                </td>
                <td>₹<%= book.price.toFixed(2) %>
                </td>
                <td style="text-align:left">
                    <%= book.description %>
                </td>
                <td>
                    <%= book.publisher %>
                </td>
                <td>
                    <%= book.year %>
                </td>

                <td>
                    <a href="/editbook/<%= book.id %>">
                        <button class="action-btn edit"><i class="fas fa-edit"></i> Edit</button></a>

                    <a href="/deletebook/<%= book.id %>">
                        <button class="action-btn delete"><i class="fas fa-trash"></i> Delete</button></a>
                </td>

            </tr>
            <% }) %>
    </table>

    <footer>
        <i class="fas fa-copyright"></i> 2025 Bookstore Management System.
    </footer>

</body>

</html>